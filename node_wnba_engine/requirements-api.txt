# Lean dependency set for the Railway inference API (serve.py).
# Excludes the sentiment scrapers (praw/twscrape) and Windows-only winotify
# so the container image builds fast and small. Use the full requirements.txt
# only if you also run the ingestion / sentiment / 24-7 loop in the same image.
duckdb>=1.1.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
joblib>=1.3.0
requests>=2.31.0
python-dotenv>=1.0.0
nba_api>=1.5.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
